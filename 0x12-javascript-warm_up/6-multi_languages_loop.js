#!/usr/bin/node
// Prints 3 lines but by using an array of string and a loop

const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let ab = 0; ab < languages.length; ab++) {
  console.log(languages[ab]);
}
